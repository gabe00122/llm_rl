# Pyrefly configuration

# Paths to include in type checking
project-includes = [
    "python/**/*.py",
    "tests/**/*.py"
]

# Paths to search for imports
search_path = [
    "python"
]

# Paths to exclude
project-excludes = [
    ".venv",
    "target",
    "build",
    "dist"
]
